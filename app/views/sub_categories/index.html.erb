<h1>Liste des sous-catégories</h1>
    <ul>
        <% @sub_categories.each do |sub_category| %>
            <li><%= link_to sub_category.name, sub_category_path(sub_category.slug) %> (<%= sub_category.category.name %>)</li>
        <% end %>
    </ul>

